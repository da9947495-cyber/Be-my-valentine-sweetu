<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For My Anshuu 🧿</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Playfair+Display:ital,wght@0,600;1,600&family=Quicksand:wght@500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --pastel-blue: #D1E9F6; /* Beautiful Pastel Blue */
            --envelope-color: #ffffff;
            --paper-color: #fdfdfd;
            --accent-pink: #ff85a2;
        }

        body {
            margin: 0;
            padding: 0;
            background-color: var(--pastel-blue);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
            font-family: 'Quicksand', sans-serif;
        }

        /* Envelope Logic */
        .container {
            position: relative;
            cursor: pointer;
            z-index: 10;
        }

        .envelope {
            position: relative;
            width: 300px;
            height: 200px;
            background: var(--envelope-color);
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            display: flex;
            justify-content: center;
            align-items: center;
            transition: 0.5s;
        }

        /* Envelope Flap */
        .envelope::before {
            content: "";
            position: absolute;
            top: 0;
            z-index: 3;
            border-top: 110px solid #f0f0f0;
            border-left: 150px solid transparent;
            border-right: 150px solid transparent;
            transition: 0.5s;
            transform-origin: top;
        }

        .stamp {
            width: 50px;
            height: 50px;
            background: #d32f2f;
            border-radius: 50%;
            z-index: 4;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            font-size: 20px;
            box-shadow: 0 0 10px rgba(0,0,0,0.2);
            font-family: serif;
        }

        /* Letter Inside */
        .letter {
            position: absolute;
            bottom: 0;
            width: 270px;
            background: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0,0,0,0.05);
            z-index: 2;
            transition: 0.8s;
            text-align: center;
            opacity: 0;
        }

        /* Animation when opened */
        .active .envelope::before {
            transform: rotateX(180deg);
            z-index: 1;
        }

        .active .letter {
            transform: translateY(-160px);
            opacity: 1;
            z-index: 5;
            width: 320px;
        }

        .active .envelope {
            transform: translateY(80px);
        }

        /* Content inside Letter */
        .puppy-sticker {
            width: 80px;
            margin-bottom: 10px;
        }

        .question {
            font-family: 'Dancing Script', cursive;
            font-size: 1.6rem;
            color: #d81b60;
            margin: 10px 0;
        }

        .btn-group {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 15px;
        }

        button {
            padding: 8px 25px;
            border-radius: 20px;
            border: none;
            cursor: pointer;
            font-weight: bold;
            font-family: 'Quicksand', sans-serif;
            transition: 0.3s;
        }

        .yes-btn {
            background-color: var(--accent-pink);
            color: white;
            box-shadow: 0 4px 10px rgba(255, 133, 162, 0.3);
        }

        /* Success Message Overlay */
        #final-screen {
            display: none;
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(255, 255, 255, 0.96);
            z-index: 100;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
            text-align: center;
            overflow-y: auto;
        }

        .note-content {
            font-family: 'Playfair Display', serif;
            font-size: 1.1rem;
            line-height: 1.8;
            color: #444;
            max-width: 600px;
            margin-top: 20px;
            background: #fff;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
            border: 1px solid #eee;
        }

        /* Falling Emojis */
        .emoji {
            position: fixed;
            top: -50px;
            font-size: 2.5rem;
            z-index: 101;
            pointer-events: none;
            animation: fall 4s linear forwards;
        }

        @keyframes fall {
            to { transform: translateY(110vh) rotate(360deg); }
        }

        /* Floating Balloons */
        .balloon {
            position: absolute;
            font-size: 25px;
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }
    </style>
</head>
<body>

    <audio id="bgMusic" loop>
        <source src="AUD-20260214-WA0002.mp3" type="audio/mpeg">
    </audio>

    <div class="container" id="envelopeContainer" onclick="openEnvelope()">
        <div class="envelope">
            <div class="stamp">❤</div>
        </div>
        <div class="letter">
            <div class="balloon" style="left: -20px; top: -20px;">🎈</div>
            <div class="balloon" style="right: -20px; top: -30px;">🎈</div>
            <img class="puppy-sticker" src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExM3ZpOWFqZ3RrYmJmNTR4ZXRvZnpxbm15bTFqY3p3Y2ZyeG5zdzJiaSZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/L95W4MsvRKXp6/giphy.gif" alt="Puppy Eyes">
            <div class="question">Will you be my Valentine, Anshuu?</div>
            <div class="btn-group">
                <button class="yes-btn" onclick="showSuccess(event)">Yes</button>
                <button id="noBtn" class="yes-btn" onmouseover="changeToYes()" onclick="showSuccess(event)">No</button>
            </div>
        </div>
    </div>

    <div id="final-screen">
        <h1 style="font-family: 'Dancing Script', cursive; color: #d81b60; font-size: 3rem; margin: 0;">Yes! ❤️</h1>
        <div class="note-content">
            "Thanks my sweetheart for choosing me I will always make sure to be by your side forever, 
            Happy valentine's day i love you so muchh my anshuu, u don't know how blessed i feel 
            to have a boyfriend like you in my life, you are everything I need, caring, lovely, 
            smart, handsome, loyal, kind, gentleman for me you are very perfect i can't describe 
            what i feel for you all ik that i really love you more than anything i promise u won't 
            see me cheating on you i will worship ur existence too 🫠🧿💞"
        </div>
    </div>

    <script>
        const music = document.getElementById('bgMusic');

        function openEnvelope() {
            document.getElementById('envelopeContainer').classList.add('active');
            music.play().catch(err => console.log("Audio waiting for interaction..."));
        }

        // Trick: Change 'No' to 'Yes' automatically
        function changeToYes() {
            const noBtn = document.getElementById('noBtn');
            noBtn.innerText = "Yes";
            noBtn.style.backgroundColor = "#ff85a2";
        }

        function showSuccess(e) {
            e.stopPropagation(); // Prevents clicking the envelope again
            document.getElementById('envelopeContainer').style.display = 'none';
            document.getElementById('final-screen').style.display = 'flex';
            
            // Shower of Kisses and Flowers
            const emojis = ['💋', '🌸', '💐', '💗', '🌷', '✨', '🧿'];
            setInterval(() => {
                const el = document.createElement('div');
                el.className = 'emoji';
                el.innerText = emojis[Math.floor(Math.random() * emojis.length)];
                el.style.left = Math.random() * 100 + 'vw';
                el.style.animationDuration = (Math.random() * 2 + 2) + 's';
                document.body.appendChild(el);
                setTimeout(() => el.remove(), 4000);
            }, 100);
        }
    </script>
</body>
</html>
